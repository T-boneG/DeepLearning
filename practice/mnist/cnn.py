#!/usr/bin/env python
"""
Convolutional Neural Nets

with depth of the network...
height and width decrease, but channels increases
conv > pool > conv > pool > FC > FC

Residual Networks
    I thought the idea was to transfer learn: take a pre-trained network and roughly double the size but make the new
    layers unity layers. Then train the new network allowing the new layers to become non-zero if so desired.
"""
